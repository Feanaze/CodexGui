# 安全策略

## 报告安全漏洞

请**不要**通过公开 Issue 报告安全问题。请使用 GitHub 仓库的 **Security → Report a vulnerability**（私密漏洞报告）功能，或直接联系维护者，并在标题里注明 `[SECURITY]`。

请在报告中提供：

- 受影响的版本 / 提交号、Windows 版本
- 复现步骤或 PoC（越小越好）
- 影响范围评估（例如：是否可导致任意命令执行、越权读写、密钥泄露）
- 你希望的披露时间（默认为修复发布后公开）

我们会在 **3 个工作日内**确认收到，并在修复可用后于 Release Notes 中致谢（除非你希望匿名）。

## 支持范围

只对最新 Release 以及 `main` 分支的最新提交提供安全修复。

## 设计上的安全边界

请理解本项目的定位：**它是一个本地桌面外壳，会以你的身份启动 Codex CLI 并让 Agent 读写文件、执行命令。**

- 默认沙箱为 `workspace-write`：Agent 只能改动当前工作目录下的文件；切到 `danger-full-access` 后不再受限，请自行评估风险。
- `auth.json`、`config.toml` 由 Codex CLI 读取，密钥以明文形式保存在本机数据目录（`%LOCALAPPDATA%\CodexGui\codex-home` 或便携模式下的 `<程序目录>\data\codex-home`）。任何能读取你用户目录的进程都能读到它，这是 Codex CLI 的既有设计，不是本项目额外的弱点。
- 本项目不会把你的密钥、会话内容发送到除你配置的 API 服务商之外的任何地方；它没有遥测、没有自动更新、没有账号系统。

## 已知的非安全问题

以下情况不属于安全漏洞，请不要作为漏洞报告：

- 你自己在 Issue / 截图 / 日志里贴出了密钥（请立刻到服务商后台吊销并重建）。
- 使用了来源不明的 `codex.exe`、第三方代理或中转 API。
- 使用 `danger-full-access` 后 Agent 按你的指令修改了系统文件。
